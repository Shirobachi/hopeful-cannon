# Generated by Gestures 0.2.3  -->  https://gitlab.com/cunidev/gestures
# Manual editing might result in data loss!


# Invalid lines
#I: gesture swispe right 3	xdotool key super+Right
#I: gesture swipe right_up 3 xdotool key shift+super+Up
#I: gesture swipe right_down 3 xdotool key shift+super+Down


# Unsupported lines

# Swipe threshold (0-100)
swipe_threshold 0

# Gestures
gesture swipe down 3 playerctl play-pause
gesture swipe up 3 playerctl play-pause
gesture swipe left 3 playerctl previous
gesture swipe right 3 playerctl next
gesture swipe left 4 i3-msg 'workspace prev'
gesture swipe right 4 i3-msg 'workspace next' 
#gesture swipe down 4 "brightnessctl -d $(sudo brightnessctl -l | head -2 | tail -1 | cut -d ' ' -f 2 | cut -d '\'' -f 2`) set 5%+"

